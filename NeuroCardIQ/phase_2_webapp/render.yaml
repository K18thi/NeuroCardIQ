services:
  - type: web
    name: neurocardiq
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn app:app
    envVars:
      - key: SECRET_KEY
        generateValue: true
      - key: DATABASE_URL
        fromDatabase:
          name: neurocardiq-db
          property: connectionString
      - key: FLASK_ENV
        value: production

databases:
  - name: neurocardiq-db
    plan: free